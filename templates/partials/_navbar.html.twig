
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
    <img src="/logo2.png" alt="Bootstrap" width="120" height="80">    
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="/">Home
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        {% for cat in categories %}
    
        <li class="nav-item">
          <a 
            class="nav-link  "  
            href="/categorie/{{cat.id}}"
          >
            {{ cat.CategorieNom }}
          </a>
        </li>

      {% endfor %}
        
      </ul>
      <div >
        <a href="/panier" class="panierinfo ">Panier >>            
          {% set total = 0 %}
                            {% for item in panier %}
                                {% set total = total + item.quantite %}
                            {% endfor %}
                            {{ total }}
          produit</a>
      </div>
      <div >      
        {% if app.user %}
          <a  class="navco" href="/logout"> se déconnecter</a>          
        {% else %}
          <a  class="navco" href="/login"> se connecter</a>          
        {% endif %}
      </div>      
      <form class="d-flex">
        <input class="form-control me-sm-2" type="text" placeholder="Search">
        <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>



